package elevio

func orderCost(orderType OrderType, orderFloor int, elevID int) int {
	// finds the cost for elevator e to do order o
	return 0
}

func findBestElevForOrder(orderType OrderType, orderFloor int, functionalElevs []int) (bestElev int, cost int) {

	return 0, 0
}

func distributeOrders() {
	// iterate through every order, check if it needs assignment and find cost
}
